# Configuración centralizada de atajos de teclado (Keybindings)
# ~/.zshrc.d/keybindings.zsh

# --- Keybindings para Búsqueda en Historial ---
# Conecta los comandos 'Ctrl + P' y 'Ctrl + N' al historial específico de un comando
bindkey '^p' history-search-backward
bindkey '^n' history-search-forward
